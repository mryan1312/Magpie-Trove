<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="MagpieTrove.Views.InputDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Magpie Trove" Height="164" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False" Background="{StaticResource BgBrush}" Foreground="{StaticResource TextBrush}" xmlns:views="clr-namespace:MagpieTrove.Views;assembly=MagpieTrove">
  <Border BorderBrush="{StaticResource LineBrush}" BorderThickness="1" Padding="16">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <TextBlock Name="PromptText" Grid.Row="0" Margin="0,0,0,8" TextWrapping="Wrap" />
      <TextBox Name="ValueBox" Grid.Row="1" Padding="8,6" KeyDown="OnValueKeyDown" />
      <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
        <Button Content="Cancel" Width="84" IsCancel="True" Click="OnCancel" />
        <Button Content="OK" Width="84" Margin="8,0,0,0" IsDefault="True" Background="{StaticResource AccentSoftBrush}" Click="OnOk" />
      </StackPanel>
    </Grid>
  </Border>
</Window>